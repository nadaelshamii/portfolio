Header always set X-Frame-Options DENY
Header always set X-Content-Type-Options nosniff
Header always set Referrer-Policy no-referrer-when-downgrade
Header always set Permissions-Policy "geolocation=(), camera=(), microphone=()"# 1. clone / drop files
git init && git add . && git commit -m "initial"

# 2. GitHub new repo → push
gh repo create portfolio --public --source=. --remote=origin
git push -u origin main

# 3. Turn on GitHub Pages (or Netlify) → site live